name: Release

# Builds the macOS release binary of the app and attaches it to a GitHub
# Release. Trigger by pushing a version tag, e.g. `git tag v0.1.0 && git push
# origin v0.1.0`, or run manually from the Actions tab (artifact only, no
# release). The site deploy is handled separately by page.yml.
# Note: the binary is unsigned — users must right-click → Open (Gatekeeper).
on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-latest # Apple Silicon (aarch64-apple-darwin)
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-release-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-release-

      - name: Build release binary
        run: cargo build --release --locked

      - name: Package
        run: |
          mkdir -p dist
          tar -czf dist/silly-aarch64-apple-darwin.tar.gz -C target/release silly

      - name: Attach to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.tar.gz
          generate_release_notes: true

      - name: Upload artifact (manual runs)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: silly-aarch64-apple-darwin
          path: dist/*.tar.gz
